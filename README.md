# Iegor Vyshnevskyi — academic website

This is a dependency-free static website published with GitHub Pages.

The root HTML files are the authoritative page sources. Shared presentation and behavior live in `assets/css/site.css` and `assets/js/site.js`; images and downloadable documents are organized under `assets/`.
